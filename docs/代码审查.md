## 2026-07-10 11:22

# 真实 API 与 SQL 缓存集成审查

## 审查结论

本轮未发现阻断提交的缺陷。当前实现满足三项关键边界：浏览器只调用站内 `/api/v1/home`；第三方凭证仅在后端环境变量中使用；所有第三方响应缓存均持久化到 SQL `api_cache` 表，未使用进程内业务缓存。

## 已验证项

- `npm run typecheck` 通过。
- `npm run build` 通过。
- `GET /api/v1/health` 返回 `status: ok`，本地数据库模式为 `pglite`。
- `GET /api/v1/home` 返回 5 个 Hero、4 个各 6 条的影视分区和 3 条 IMDb 新闻，所有八个来源操作均成功。
- SQL 中已持久化 TMDB、豆瓣、IMDb 缓存记录和 Provider 同步日志，进程重启后仍可命中。
- 首页桌面端真实标题、图片、IMDb 评分、缺失评分状态和新闻均正确渲染，浏览器控制台无错误或警告，页面无横向溢出。
- 源码扫描确认前端仅在 `src/services/home-api.ts` 调用 `/api/v1/home`，没有第三方 API 地址或密钥。

## 剩余风险

- 首次冷缓存需要调用多个上游接口，耗时受第三方服务影响；已将 Hero 富化限制为随机五部并使用 SQL TTL 缓存降低后续请求成本。
- 当前跨源评分采用保守标题与年份匹配，宁可显示“暂无”也不制造错误评分；后续详情页应通过持久化外部 ID 映射提升命中率。
- 当前仅完成首页聚合接口；搜索、探索、详情、登录和管理员后台仍应在后续阶段按同一 Provider 与 SQL 缓存边界实现。

---

## 2026-07-10 16:03

# 开发服务重复启动端口冲突修复审查

## 问题定位

- 端口 8787 已有正常运行的 MovieScope API，重复执行 `npm run dev` 又创建一个 API 进程，因此触发 `EADDRINUSE`。
- 原联合启动脚本未探测已有服务，也未监听子进程 `error` 事件。

## 修复与验证

- 启动器通过本地 HTTP 健康检查识别 8787 上是否为 MovieScope API，并通过首页 HTML 标题识别 5173 上是否为 MovieScope 前端。
- 已存在正常 MovieScope 服务时直接复用，只启动缺失的一端；不会停止正在使用 PGlite 的正常 API。
- 端口若被其他程序占用，会输出明确中文错误并安全退出。
- API 服务器增加监听错误处理，`EADDRINUSE` 不再形成未处理的 `error` 事件堆栈。
- Windows Node 24 下使用原生 HTTP 探测并自然退出，避免 `fetch` 后立即退出产生 libuv assertion。
- 实测已有 API、缺少前端时，`npm run dev` 复用 API 并成功启动 Vite。
- 实测前后端均已运行时，再次执行 `npm run dev` 返回成功并提示 MovieScope 已在本地运行。
- `http://127.0.0.1:5173/` 返回 200，代理健康接口返回 `status: ok`、`database: pglite`。
- 已调用系统默认浏览器打开本地 MovieScope 首页。
- `npm run typecheck` 与 `npm run build` 均通过。

---

## 2026-07-10 15:51

# 上游 fetch failed 降级修复审查

## 问题定位

- 本地 Vite、MovieScope API 和 PGlite 健康检查均正常。
- `GET /api/v1/home` 原先返回 500，原因是 TMDB 类型字典请求位于 `safeSource` 容错层之外；新缓存库为空时，上游连接失败会中断整个首页聚合。
- 当前本机 DNS 将 `api.themoviedb.org` 解析到 `192.133.77.133` 和 `2a03:2880:f12c:83:face:b00c:0:25de`，443 端口连接失败，因此属于本机网络或 DNS 环境问题，而非 MovieScope API 进程不可用。

## 修复与验证

- TMDB 电影与剧集类型字典改为 `Promise.allSettled`，失败时使用空类型字典，不再让首页返回 500。
- Provider 捕获底层网络异常，错误信息改为“无法连接上游服务 + 域名 + 具体错误码”，不再直接暴露模糊的 `fetch failed`。
- 实测在 TMDB 全部请求超时、当前 SQL 无 TMDB 缓存时，`GET /api/v1/home` 仍返回 200。
- 降级响应中 IMDb Top 250 仍返回 6 条，IMDb 新闻返回 100 条，TMDB 分区为空并在 `sourceMeta` 中明确记录 `UND_ERR_CONNECT_TIMEOUT`。
- `npm run typecheck` 与 `npm run build` 均通过。

## 环境边界

- 要恢复 Hero、正在上映、热门电影和热门剧集的实时数据，需要修复本机 DNS、代理或网络对 `api.themoviedb.org:443` 的访问。
- 当 SQL 中重新积累 TMDB 缓存后，短时网络故障可继续使用已有的 SQL 过期缓存降级。

---

## 2026-07-10 15:46

# PGlite 本地启动崩溃修复审查

## 问题定位

- 用户提供的日志中 `npm run build` 已成功，失败发生在随后执行的 `npm run dev`。
- 本地 `.data/pglite` 在开发进程被强制终止后无法完成 PostgreSQL WAL 恢复，PGlite 仅输出 `RuntimeError: Aborted()` 和整段压缩库源码。
- 没有残留 MovieScope API 进程；删除 `postmaster.pid` 并在数据目录副本中启动仍然失败，证明不是普通锁文件问题。

## 恢复处理

- 故障数据目录已完整保留为：`.data/pglite-corrupt-20260710-1543`。
- 额外备份保留为：`.data/pglite-backup-20260710-1541`。
- 当前数据库只包含第三方 API 缓存和同步日志，不包含用户账户或观影记录，因此重新初始化 `.data/pglite` 不会丢失用户业务数据。
- 新数据库已完成迁移并通过健康检查，后续第三方调用会重新填充 SQL 缓存。

## 代码修复与验证

- 数据库初始化失败时会重置失败 Promise，允许后续重试。
- PGlite 出现 `Aborted`、互斥锁或锁定错误时，输出简短中文诊断和备份建议。
- API 顶层捕获数据库启动异常，不再直接打印 PGlite 的整段压缩源码。
- `npm run dev` 已同时启动 Vite 和 API；经 Vite 代理访问健康接口返回 `status: ok`、`database: pglite`。
- 使用保留的故障副本验证错误路径，只输出两行可读中文错误。
- `npm run typecheck` 与 `npm run build` 均通过。

---

## 2026-07-10 15:36

# Hero 按钮与 IMDb 新闻展示审查

## 审查结论

本轮修复了 Hero 操作按钮在中文大字号下的文字溢出，并根据 IMDb 新闻接口真实字段调整新闻卡片。新闻图片只在接口返回 `node.image.url` 时显示，不再使用 MovieScope 通用兜底图。

## 接口核查

- 当前 SQL 缓存中的 IMDb 新闻响应包含 100 条 `news.edges`。
- 响应同时返回 `total`、`pageInfo.hasNextPage=true` 和 `endCursor`，说明接口数据量大于当前缓存的 100 条。
- 新闻图片字段为 `node.image.url`，示例域名为 `m.media-amazon.com`，属于新闻接口响应中的真实图片。
- 部分新闻的 `node.image` 为 `null`；这些新闻现在使用纯文本卡片，不再显示伪造或无关封面。

## 实现与验证

- 后端不再对新闻数组执行前三条截断，完整返回当前接口响应的新闻列表。
- 前端默认展示 3 条，每次点击“查看更多”增加 6 条，直到当前列表全部显示。
- Hero 两个按钮增加明确最小宽度、`whitespace-nowrap`、居中布局和不可收缩文字节点。
- `npm run typecheck` 通过。
- `npm run build` 通过。
- 本地正式 PGlite 在开发进程被强制中断后出现底层锁状态，因此未破坏或重建正式缓存目录；新闻字段与数量基于停止 API 后成功读取的真实 SQL 缓存完成核验。

---

## 2026-07-10 15:23

# 首页视觉尺寸轻微回调审查

## 审查结论

本轮仅对上一版压缩尺度做半档回调，未恢复到最初偏大的尺寸。顶栏、基础字号、内容宽度、Hero 文案、海报信息、新闻卡片和页脚均轻微放大。

## 验证结果

- `npm run typecheck` 通过。
- `npm run build` 通过。
- 桌面顶栏实测约 51px，上一版约 46px。
- Hero 仍为完整 `100svh`，720px 视口下实测高度为 720px。
- 页面基础字号由 15px 调整为 15.5px，内容宽度由 1180px 调整为 1216px。
- 页面无横向溢出，数据和交互逻辑未发生变化。

## 502 修复补充

- `npm run dev` 现在同时启动 MovieScope API 与 Vite，不再只启动前端导致代理返回 502。
- 单独启动前端仍可使用 `npm run dev:web`，单独启动 API 使用 `npm run api`。
- Windows 下联合启动直接通过 Node 执行 Vite 入口，避免 Node 24 启动 `npm.cmd` 时出现 `spawn EINVAL`。
- 前端对 502、503、504 显示“API 服务未启动或暂时不可用”，不再显示模糊的状态码错误。
- 实测 `http://127.0.0.1:5173/api/v1/health` 经 Vite 代理返回 `status: ok`。

---

## 2026-07-10 15:14

# 豆瓣暂停、响应解析与视觉收缩审查

## 审查结论

本轮未发现阻断提交的问题。首页已完全停止豆瓣调用和展示；空响应不再进入 `Response.json()`；全站视觉密度已收紧，同时 Hero 保持完整首屏覆盖。

## 验证结果

- `npm run typecheck` 通过。
- `npm run build` 通过。
- `GET /api/v1/home` 返回 5 个 Hero、四个各 6 条影视分区和 3 条新闻，`sourceMeta` 中没有豆瓣来源。
- 浏览器首屏顶栏实测约 46px，Hero 高度等于 720px 视口高度，页面无横向溢出。
- 页面可见文本中没有“豆瓣”，介绍文案除 IMDb Top 250 外不再说明 TMDB、IMDb 或接口来源。
- Hero 轮播等待自动切换后，图片和文案透明度均为 1；控制台无应用错误或警告。
- 当前网络短时无法直连 TMDB 时，SQL 过期缓存降级仍可完整展示首页，未引入内存缓存。

## 已知边界

- 过期缓存降级窗口限制为 30 天；没有任何历史 SQL 缓存且上游不可达时，相应分区仍会按现有容错规则显示为空。
- 顶栏在窄屏会隐藏部分文字和注册按钮，保持搜索与菜单的基础可用性；后续移动端页面阶段可继续做专门布局。

---

## 2026-07-10 14:01

# 首页评分补全修复审查

## 审查结论

本轮未发现阻断问题。IMDb 单片评分通过 TMDB External ID 精确映射，不依赖标题猜测；豆瓣评分仍采用保守标题、原始标题和年份匹配，避免错误归属。前端会明确显示 IMDb、TMDB 或“未收录”，不存在评分来源冒充。

## 验证结果

- `npm run typecheck` 通过。
- `npm run build` 通过。
- 首页五个影视区域共 29 个条目均有可展示的主评分；IMDb 当前实测覆盖全部 29 个条目。
- 豆瓣电影、豆瓣剧集、IMDb Top 250、IMDb 单片用户评价摘要接口均已恢复调用。
- `npm run cache:inspect` 可查看 SQL 缓存摘要和最近缓存键。
- `npm run cache:export -- cache-review.json` 可导出完整缓存与同步日志，导出文件已加入 Git 忽略。
- 浏览器首页显示真实 IMDb 评分，控制台无错误，页面无横向溢出。

## 已知边界

- 豆瓣近期热门 API 并不是任意影片搜索接口，因此只有落入近期热门集合且可靠匹配的影片能显示豆瓣评分。
- 本地 PGlite 同一时间只能由一个进程打开；运行核查或导出命令前需要停止 API 服务。

---

## 2026-07-10 14:35

# IMDb Provider 拆分审查

## 审查结论

本轮未发现阻断提交的问题。Just One API 的 IMDb 调用已收束为 Top 250 和新闻，其他 IMDb 评分由 `imdbapi.dev` 提供。真实接口回归中发现并修复了批量接口的故障放大问题：单个坏 IMDb ID 不再导致同批其他条目全部回退。

## 验证结果

- `npm run typecheck` 通过。
- `npm run build` 通过。
- `GET /api/v1/health` 返回 PGlite 数据库健康。
- `GET /api/v1/home` 的九个上游来源均成功，返回 5 个 Hero、四个 6 条影视分区和 3 条新闻。
- 正在上映、IMDb Top 250 和热门电影的 IMDb 评分实测均为 `6/6`；热门剧集在隔离不可用 ID 后由 `1/6` 提升为 `4/6`，其余条目明确使用 TMDB 回退。
- SQL 实测 TTL：Top 250 为 `604800` 秒，新闻和 `imdbapi.dev` 批量详情均为 `86400` 秒。
- 已删除旧 Just One 单片评分活动缓存；历史同步日志保留。新缓存键只包含 IMDb ID，不包含第三方令牌。

## 剩余风险

- `imdbapi.dev` 会对个别尚未同步或上游异常的 IMDb ID 返回 500；递归拆分可保护同批有效数据，但首次遇到坏 ID 时会增加少量请求。
- Hero 每次从 TMDB 热门内容随机抽取，IMDb 覆盖率会随抽样结果变化；没有 IMDb 数据时继续显示真实 TMDB 分数，不伪造来源。

---

## 2026-07-10 16:18

# 首页 Hero 与全站评分优先级修复审查

## 审查结论

本轮未发现阻断问题。首页 Hero 已从“先随机抽取再补评分”改为“扩大趋势候选、补全 IMDb 后过滤、再随机抽取”，并可使用正在上映与热门电影中已取得 IMDb 官方评分的项目补足。Hero 不再接收 TMDB 回退或未收录项目。全站评分展示入口已统一，存在 IMDb 评分时始终优先显示 IMDb。

## 验证结果

- `node --test server/home-service.test.mjs` 通过，覆盖 Hero IMDb 官方评分过滤、无背景图过滤与影视 ID 去重。
- `npm run typecheck` 通过。
- `npm run build` 通过。
- `git diff --check` 通过。
- 真实接口 `GET /api/v1/home` 返回 5 个 Hero，5 个项目的 IMDb 评分均非空且 `ratingDisplay.imdb` 均为 `official`。
- 本次实测 TMDB 趋势、正在上映、热门电影、热门剧集、Top Rated、IMDb Top 250 与 IMDb 新闻来源均正常。

## 已知边界

- 普通海报卡片在 IMDb 确实没有收录时，仍允许按既有规则明确标注并展示 TMDB 回退评分；IMDb 一旦存在则无条件优先。
- Hero 宁可少于 5 部也不会展示 TMDB 评分或伪造 IMDb 评分；当前真实数据回归可稳定取得 5 部。

---

## 2026-07-10 16:32

# 首页中文影视标题补全修复审查

## 审查结论

本轮未发现阻断问题。IMDb Top 250 条目现在使用 IMDb ID 通过 TMDB `/find/{imdb_id}` 精确取得简体中文正式片名；TMDB 分区中仍为外文的标题会查询影视条目的中文 translations，并按中国大陆、新加坡、台湾、香港顺序选择真实中文译名。找不到中文译名时保留上游原名，不进行机器臆造翻译。

## 验证结果

- `node --test server/home-service.test.mjs` 通过，覆盖中文字符识别、简体地区优先和空译名处理。
- `npm run typecheck` 通过。
- `npm run build` 通过。
- `git diff --check` 通过。
- 真实接口回归中，高分榜前六部已显示为“肖申克的救赎”“教父”“蝙蝠侠：黑暗骑士”“教父2”“十二怒汉”“指环王3：王者无敌”。
- 翻译与 IMDb ID 查找请求继续使用现有 SQL 缓存，TTL 为 30 天。

## 已知边界

- 截图中的 `प्रीतम और पेड्रो` 以及 `Xplay` 当前在 TMDB 的中文详情、translations 和地区别名中均没有有效中文片名，因此继续显示真实原名。
- 中文译名质量由 TMDB 已收录的地区译名决定；系统不会自行生成可能错误的中文片名。

---

## 2026-07-10 16:36

# 后端旧进程与标题更新未生效修复审查

## 根本原因

页面仍连接到 2026-07-10 16:13 启动的旧 API 进程。Vite 只热更新前端，原开发脚本启动的 Node API 不会在后端文件变化时重启，因此接口继续返回修改前的英文高分榜标题。旧 API 退出后还暴露出原 PGlite 目录处于异常状态，连完整副本也无法重新打开。

## 修改与恢复

- 开发脚本改用 Node `--watch` 启动 API，后续 `server/` 代码变化会自动重启后端。
- 原异常 PGlite 目录完整保留为 `.data/pglite-backup-corrupt-20260710-163430`，未删除缓存文件。
- 新建 `.data/pglite` SQL 缓存库并重新拉取真实 API 数据。

## 验证结果

- 当前 `GET /api/v1/home` 高分榜返回“肖申克的救赎、教父、蝙蝠侠：黑暗骑士、教父2、十二怒汉、指环王3：王者无敌”。
- 当前 TMDB 首页来源、IMDb Top 250 和 IMDb 新闻来源均为 `ok`。
- 触碰后端文件后，API 子进程 PID 从 `13516` 自动更新为 `35648`，健康检查恢复且 PGlite 可正常打开。
- `git diff --check` 通过。

---

## 2026-07-10 17:35

# 综合目录页面、真实 API 与 MySQL 迁移审查

## 审查结论

本轮未发现阻断提交的问题。三份页面均保留对应 HTML 草稿的主要结构，只替换为现有全局顶栏和页脚，并将静态内容改为真实 TMDB 与 IMDb 数据。搜索、探索、详情、人物和首页已经通过 Vue Router 联动，未保留无行为的主要按钮。

## 数据与功能验证

- MySQL 8 健康检查返回 database: mysql，api_cache 和 provider_sync_logs 已通过迁移自动创建。
- 综合搜索“沙丘”实测返回 20 条结果；探索电影首屏返回 12 条结果。
- 《肖申克的救赎》详情实测返回 18 位演员、12 张剧照、IMDb 9.3 和 3,204,662 条评分。
- 摩根·弗里曼人物详情实测返回 24 部代表作品和 12 张人物图片。
- MySQL 当前已持久化 TMDB 与 imdbapi.dev 缓存，检查与导出脚本均可执行。
- 海报、Hero、详情分类、演员、人物作品、分页、筛选、榜单入口与顶栏搜索均绑定真实路由。
- 详情页 IMDb 和 TMDB 评分均显示评分人数并可跳转来源网站；分享按钮默认复制 IMDb URL，并提供 Clipboard API 降级方案。

## 前端验证

- npm run typecheck 通过。
- npm run build 通过。
- node --test server/home-service.test.mjs 通过。
- git diff --check 通过。
- 浏览器桌面端检查：首页、搜索、探索、详情和人物页均返回 200，无横向溢出；新增四个页面无控制台错误。
- 390 × 844 移动端检查：搜索、探索和详情页均无横向溢出，筛选组件可收折。

## 已知边界

- 想看、看过和个人评分目前使用浏览器本地持久状态提供可执行交互；将在用户登录与用户数据阶段迁移到 MySQL 用户表。
- 豆瓣接口当前暂停，因此详情草稿中的豆瓣评分和中文评论不展示；TMDB 公共评论可能为外文原文。
- 搜索页草稿中的运行时长无法由搜索接口直接返回，结果列表不额外为每条影片请求详情，避免产生大量请求；运行时在详情页完整展示。

---

## 2026-07-10 18:42

# 顶栏菜单与探索入口功能修复审查

## 根本原因

顶栏探索、高级搜索仍使用首页锚点，跨页面点击没有效果；菜单项目依赖文字集合和兜底搜索映射，导致“正在上映”“今日播出”“观看平台”“热门影人”等入口无法执行对应真实业务。

## 修改内容

- 顶栏探索与高级搜索改为 Vue Router 的 /explore 路由。
- 菜单全部改为显式路由映射，不再把菜单名称当普通搜索词。
- 新增通用榜单页，承载正在上映、即将上映、热门/高分电影与剧集、今日播出、今日趋势和本周趋势。
- 新增热门影人页，并支持演员、导演与编剧职业过滤。
- 新增观看平台页，展示 51 个 TMDB 中国大陆、香港或台湾地区可用服务商，并可按平台进入电影或剧集探索。
- 新增影坛动态页，展示 IMDb 新闻接口当前返回的 100 条真实资讯。
- 暂无用户系统或社区接口的入口跳转明确的建设中页面；登录与注册显示可访问状态提示。

## 验证结果

- npm run typecheck、npm run build、现有 Node 测试和 git diff --check 均通过。
- 真实接口：正在上映 12 条、即将上映 6 条、今日播出 12 条、今日趋势 12 条、热门影人 20 条、观看平台 51 条、新闻 100 条。
- 浏览器逐项点击确认：探索进入 /explore；正在上映进入 /browse/now-playing；热门影人进入 /people；观看平台进入 /providers；影坛动态进入 /news；高级搜索进入 /explore。
- 未开放的中文短评进入明确提示页，登录按钮显示“用户系统阶段接入”反馈。
- 新增页面桌面端无控制台错误；观看平台页在 390 像素宽度无横向溢出。

---


## 2026-07-10 21:35

### 影视检索页面原稿回归与 IMDb 详情修复

- 修复首页 IMDb Top 250 使用 tt IMDb ID 跳转后详情接口 404：服务端现通过 TMDB find 接口精确解析到 TMDB ID，再加载统一详情。
- 影视详情 Hero 恢复原稿的 70vh / 85vh 横向 backdrop 结构，竖版 poster 仅作为前景海报；删除剧情下方英文关键词标签。
- 详情图片按 file_path 去重，横向剧照最多返回与展示 24 张；增加海报、Logo 及剧集主创、季集数、播出日期等字段。
- 媒体口碑、你的互动和影视详情恢复原稿层次，其中影视详情不再套统一卡片外框，并补充上映状态、公司、地区、语言、时长、分级、预算、票房和 IMDb ID。
- 搜索页恢复结果卡片位移、海报缩放、标题变色、收藏选中态和分页反馈；探索页恢复原稿横向筛选行、五列海报网格、右侧热门榜单及筛选选中态。
- 全局海报评分不再回退显示 TMDB，IMDb 缺失时明确显示 IMDb 暂无。

### 验证

- `npm.cmd run typecheck`：通过。
- `npm.cmd run build`：通过。
- `node --test server/home-service.test.mjs`：2 项通过。
- `GET /api/v1/titles/movie/tt0111161`：返回 200，解析为 TMDB 278，IMDb 9.3，横向 backdrop 正常，24 张剧照路径互不重复。
- 本机 Edge 可完成三个路由的 DOM 与真实数据加载；受安全策略限制，外部 Tailwind CDN 与图片域名在无头浏览器中被拦截，因此未将该无样式截图作为视觉验收依据。


## 2026-07-10 23:10

### 导航去重、目录数据完整性与列表页交互修复

- 顶栏子菜单去除同义重复入口：电影与剧集高分榜只保留所属分类，榜单区保留 IMDb Top 250 与全球票房榜；影人区合并为热门影人、热门演员、导演与编剧；影坛动态只保留一个入口。
- 当前 API 能稳定支持票房排序，但没有已接入的结构化奖项或影展专题数据，因此移除奖项与影展入口，未伪造专题内容。
- IMDb Top 250 改为独立可分页榜单页，每页 25 部，通过 IMDb ID 跳转详情，并使用精确 TMDB 匹配补充中文标题和海报。
- 搜索、探索、普通榜单及详情推荐改为整页补全 IMDb 数据；批量接口失败时递归拆分并使用单片接口兜底。只有 IMDb ID 或官方评分确实不可用时才显示 TMDB 回退评分。
- 即将上映改为合并未来 5 页档期并按日期排序，严格排除早于当前日期的作品。
- 新闻页增加标题、来源与摘要搜索，接入新闻真实图片，标题区仅保留一条分隔线并移除接口来源介绍句。
- 热门人物每页最多 30 人并恢复卡片悬浮动效；热门演员从多页热门人物聚合，导演与编剧从热门电影真实演职员表聚合。
- 观看平台携带真实可用地区参数进入 discover 筛选；详情页按订阅、租赁、购买分组显示平台，已知平台直达官网，未知平台回退到供应商提供的观看信息页。
- 新增统一数字分页组件，支持相邻页码、省略号、首尾页、禁用态及移动端尺寸。

### 验证

- `npm.cmd run typecheck`、`npm.cmd run build`、`node --test server/home-service.test.mjs` 均通过。
- IMDb Top 250：250 条，首页 25 条，首条《肖申克的救赎》IMDb 9.3。
- 即将上映：首页 20 条，最早日期不早于 2026-07-10，过期条目为 0。
- 搜索《教父》前五部均取得精确 IMDb ID 与评分；热门电影 20/20 取得 IMDb 官方评分。
- 新闻接口返回 100 条，其中 98 条包含真实图片。
- 热门导演与编剧返回 26 人，职业来自 Directing 与 Writing；热门演员返回 30 人。
- Netflix 香港区筛选实际返回 20 部作品，响应保留 provider=8、watchRegion=HK。
